package com.obscura.kit

import org.junit.jupiter.api.Assertions.assertNotNull
import org.junit.jupiter.api.Test

/**
 * Placeholder unit test so :lib:test has at least one test class to run.
 *
 * Real unit tests for pure-logic surfaces (CRDT merge semantics, QueryBuilder,
 * BIP39, FriendCode, UuidCodec, Schema construction, etc.) belong here.
 * See `lib/src/integrationTest/` for the existing server-dependent scenario suite.
 */
class PlaceholderUnitTest {
    @Test
    fun `unit test suite is wired`() {
        assertNotNull("ok")
    }
}
